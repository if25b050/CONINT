services:
  database:
    image: postgres:17.4-alpine3.21
    ports:
      - "127.0.0.1:5432:5432"
    env_file:
      - .env
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '3'
    volumes:
      - ping-postgres-data:/var/lib/postgresql/data

  migrations:
    build: .
    command: sh -c "npm ci --save-dev && npm run migrate"
    depends_on:
      - database
    env_file:
      - .env
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '3'
  redis:
    image: redis:8.6.2-alpine
    ports:
      - "127.0.0.1:6379:6379"
    env_file:
      - .env
    volumes:
      - ping-redis-data:/data

  web:
    build: .
    ports:
      - "127.0.0.1:5000:5000"
    depends_on:
      - database
      - migrations
      - redis
    env_file:
      - .env
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '3'

volumes:
  ping-postgres-data:
  ping-redis-data:
